###################################################################################################

dependencies:

  -   role: shared

  - { role: platform/ssh/common, ssh_user : '{{ ssh_agent_user  }}' ,
                                 ssh_group: '{{ ssh_agent_group }}' }

###################################################################################################
